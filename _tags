# true: use_unix, thread, debug
# true: thread, debug

# Required so the object file is compiled with -thead
<basic_echo_server.ml> : thread, debug
# Required so the binary produced has -thread support
<basic_echo_server.{byte,native}> : thread, debug
